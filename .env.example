# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# Directory the capstone notes server may read/write (least privilege).
NOTES_DIR=./notes

# Example: an API key a tool might need (loaded from the environment,
# never hardcoded in source).
# WEATHER_API_KEY=your-key-here
